# Frontend environment example for OceanKicks (Create React App)
# Copy this file to .env and adjust values as needed.

# Base URL of the backend API (no trailing slash).
# Leave empty to default to mock data.
REACT_APP_API_BASE_URL=

# "true" to force mocks, "false" to force real API.
# If not explicitly set to "true" or "false", mocks are used when REACT_APP_API_BASE_URL is empty.
REACT_APP_USE_MOCKS=true
